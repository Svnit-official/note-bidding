import {makeStyles} from '@material-ui/core/styles';

export default makeStyles({
    button : {
        zIndex : 7,
        fontSize : '100px',
        right : '0px',
        position : 'static',
    }
})