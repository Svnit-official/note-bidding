import {makeStyles} from '@material-ui/core/styles';

export default makeStyles({
    button : {
        zIndex : 7,
        fontSize : '100px',
        left : '0px',
        position : 'relative',
    }
})